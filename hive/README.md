# Apache Hive

This folder should contain the Hive SQL used in your project.

Recommended files:

- `create_tables.sql`
- `queries.sql`
- any additional `.sql` files needed for your final implementation

Also replace `screenshots/hive-query-results.png` with a representative screenshot of successful output.

Your README should explain what the Hive tables represent and what the selected queries demonstrate.
